{
  "buildCommand": "cd demo-app/frontend && npm install && npm run build",
  "outputDirectory": "demo-app/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
